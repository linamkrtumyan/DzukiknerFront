import React from "react";
import { Button } from "react-bootstrap";

function AddFeeding() {
  return (
    <div>
      <Button variant="primary">Հաստատել</Button>
    </div>
  );
}

export default AddFeeding;
